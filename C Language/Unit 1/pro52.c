#include<stdio.h>

void main(){
    /**
        sizeof operator used to get size of any variable in bytes
    */

    printf("\n Size of integer variable is %d Bytes", sizeof(int));
    printf("\n Size of short integer variable is %d Bytes", sizeof(short int));
    printf("\n Size of character variable is %d Bytes", sizeof(char));
}
